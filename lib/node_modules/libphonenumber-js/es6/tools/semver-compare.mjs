"use client";
function f(r, t) {
  r = r.split("-"), t = t.split("-");
  for (var s = r[0].split("."), N = t[0].split("."), e = 0; e < 3; e++) {
    var i = Number(s[e]), u = Number(N[e]);
    if (i > u)
      return 1;
    if (u > i)
      return -1;
    if (!isNaN(i) && isNaN(u))
      return 1;
    if (isNaN(i) && !isNaN(u))
      return -1;
  }
  return r[1] && t[1] ? r[1] > t[1] ? 1 : r[1] < t[1] ? -1 : 0 : !r[1] && t[1] ? 1 : r[1] && !t[1] ? -1 : 0;
}
export {
  f as default
};
