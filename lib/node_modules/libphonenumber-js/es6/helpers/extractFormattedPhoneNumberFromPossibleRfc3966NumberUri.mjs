"use client";
import s, { isPhoneContextValid as x, PLUS_SIGN as F, RFC3966_PREFIX_ as a, RFC3966_PHONE_CONTEXT_ as c, RFC3966_ISDN_SUBADDRESS_ as d } from "./extractPhoneContext.mjs";
import l from "../ParseError.mjs";
function C(r, _) {
  var f = _.extractFormattedPhoneNumber, e = s(r);
  if (!x(e))
    throw new l("NOT_A_NUMBER");
  var t;
  if (e === null)
    t = f(r) || "";
  else {
    t = "", e.charAt(0) === F && (t += e);
    var n = r.indexOf(a), o;
    n >= 0 ? o = n + a.length : o = 0;
    var h = r.indexOf(c);
    t += r.substring(o, h);
  }
  var i = t.indexOf(d);
  if (i > 0 && (t = t.substring(0, i)), t !== "")
    return t;
}
export {
  C as default
};
