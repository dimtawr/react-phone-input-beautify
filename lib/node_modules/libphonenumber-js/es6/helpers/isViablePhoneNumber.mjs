"use client";
import { MIN_LENGTH_FOR_NSN as E, VALID_DIGITS as _, PLUS_CHARS as t, VALID_PUNCTUATION as e } from "../constants.mjs";
import r from "./extension/createExtensionPattern.mjs";
var n = "[" + _ + "]{" + E + "}", I = "[" + t + "]{0,1}(?:[" + e + "]*[" + _ + "]){3,}[" + e + _ + "]*", P = new RegExp("^[" + t + "]{0,1}(?:[" + e + "]*[" + _ + "]){1,2}$", "i"), R = I + // Phone number extensions
"(?:" + r() + ")?", T = new RegExp(
  // Either a short two-digit-only phone number
  "^" + n + "$|^" + R + "$",
  "i"
);
function A(N) {
  return N.length >= E && T.test(N);
}
function o(N) {
  return P.test(N);
}
export {
  I as VALID_PHONE_NUMBER,
  R as VALID_PHONE_NUMBER_WITH_EXTENSION,
  A as default,
  o as isViablePhoneNumberStart
};
