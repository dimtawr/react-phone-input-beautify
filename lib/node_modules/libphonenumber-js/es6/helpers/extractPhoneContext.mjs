"use client";
import { VALID_DIGITS as r } from "../constants.mjs";
var i = "+", v = "[\\-\\.\\(\\)]?", R = "([" + r + "]|" + v + ")", I = "^\\" + i + R + "*[" + r + "]" + R + "*$", N = new RegExp(I, "g"), n = r, C = "[" + n + "]+((\\-)*[" + n + "])*", L = "a-zA-Z", f = "[" + L + "]+((\\-)*[" + n + "])*", l = "^(" + C + "\\.)*" + f + "\\.?$", u = new RegExp(l, "g"), O = "tel:", a = ";phone-context=", P = ";isub=";
function s(_) {
  var t = _.indexOf(a);
  if (t < 0)
    return null;
  var e = t + a.length;
  if (e >= _.length)
    return "";
  var A = _.indexOf(";", e);
  return A >= 0 ? _.substring(e, A) : _.substring(e);
}
function D(_) {
  return _ === null ? !0 : _.length === 0 ? !1 : N.test(_) || u.test(_);
}
export {
  i as PLUS_SIGN,
  P as RFC3966_ISDN_SUBADDRESS_,
  a as RFC3966_PHONE_CONTEXT_,
  O as RFC3966_PREFIX_,
  s as default,
  D as isPhoneContextValid
};
