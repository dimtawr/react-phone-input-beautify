"use client";
import l from "../metadata.mjs";
import { VALID_DIGITS as I } from "../constants.mjs";
var c = new RegExp("([" + I + "])");
function s(t, r, f, n) {
  if (r) {
    var a = new l(n);
    a.selectNumberingPlan(r, f);
    var i = new RegExp(a.IDDPrefix());
    if (t.search(i) === 0) {
      t = t.slice(t.match(i)[0].length);
      var e = t.match(c);
      if (!(e && e[1] != null && e[1].length > 0 && e[1] === "0"))
        return t;
    }
  }
}
export {
  s as default
};
