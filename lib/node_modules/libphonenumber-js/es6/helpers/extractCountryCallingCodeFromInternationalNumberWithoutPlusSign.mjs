"use client";
import N, { getCountryCallingCode as m } from "../metadata.mjs";
import u from "./matchesEntirely.mjs";
import f from "./extractNationalNumber.mjs";
import a from "./checkNumberLength.mjs";
function v(r, e, o, n) {
  var i = e ? m(e, n) : o;
  if (r.indexOf(i) === 0) {
    n = new N(n), n.selectNumberingPlan(e, o);
    var t = r.slice(i.length), C = f(t, n), c = C.nationalNumber, g = f(r, n), l = g.nationalNumber;
    if (!u(l, n.nationalNumberPattern()) && u(c, n.nationalNumberPattern()) || a(l, n) === "TOO_LONG")
      return {
        countryCallingCode: i,
        number: t
      };
  }
  return {
    number: r
  };
}
export {
  v as default
};
