"use client";
import s from "./createExtensionPattern.mjs";
var n = new RegExp("(?:" + s() + ")$", "i");
function o(e) {
  var a = e.search(n);
  if (a < 0)
    return {};
  for (var i = e.slice(0, a), r = e.match(n), t = 1; t < r.length; ) {
    if (r[t])
      return {
        number: i,
        ext: r[t]
      };
    t++;
  }
}
export {
  o as default
};
