"use client";
function F(l, i) {
  if (l && i.numberingPlan.nationalPrefixForParsing()) {
    var g = new RegExp("^(?:" + i.numberingPlan.nationalPrefixForParsing() + ")"), n = g.exec(l);
    if (n) {
      var e, r, P = n.length - 1, v = P > 0 && n[P];
      if (i.nationalPrefixTransformRule() && v)
        e = l.replace(g, i.nationalPrefixTransformRule()), P > 1 && (r = n[1]);
      else {
        var o = n[0];
        e = l.slice(o.length), v && (r = n[1]);
      }
      var f;
      if (v) {
        var s = l.indexOf(n[1]), a = l.slice(0, s);
        a === i.numberingPlan.nationalPrefix() && (f = i.numberingPlan.nationalPrefix());
      } else
        f = n[0];
      return {
        nationalNumber: e,
        nationalPrefix: f,
        carrierCode: r
      };
    }
  }
  return {
    nationalNumber: l
  };
}
export {
  F as default
};
