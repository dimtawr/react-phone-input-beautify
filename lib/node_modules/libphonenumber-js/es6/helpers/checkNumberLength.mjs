"use client";
import f from "./mergeArrays.mjs";
function _(i, r) {
  return u(i, void 0, r);
}
function u(i, r, n) {
  var t = n.type(r), e = t && t.possibleLengths() || n.possibleLengths();
  if (!e)
    return "IS_POSSIBLE";
  if (r === "FIXED_LINE_OR_MOBILE") {
    if (!n.type("FIXED_LINE"))
      return u(i, "MOBILE", n);
    var I = n.type("MOBILE");
    I && (e = f(e, I.possibleLengths()));
  } else if (r && !t)
    return "INVALID_LENGTH";
  var L = i.length, s = e[0];
  return s === L ? "IS_POSSIBLE" : s > L ? "TOO_SHORT" : e[e.length - 1] < L ? "TOO_LONG" : e.indexOf(L, 1) >= 0 ? "IS_POSSIBLE" : "INVALID_LENGTH";
}
export {
  u as checkNumberLengthForType,
  _ as default
};
